{
  "name": "zip-code-embedded",
  "version": "1.0.0",
  "description": "Zip Code Lat Long Lookup",
  "main": "index.js",
  "scripts": {
    "build": "npx webpack --mode=production && npx mkdirp target && spin js2wasm -o target/zip-code-embedded.wasm dist/spin.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mkdirp": "^3.0.1",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "@fermyon/spin-sdk": "^0.6.0"
  }
}
